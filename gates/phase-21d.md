# Gates: Floe Phase 21D — Release Candidate

Scope: Audited, recoverable, reproducible release candidate without later privileged, Undo, sandbox, or filesystem-fidelity features.

- [x] R1: Every shipped Rust package has an allowed declared license and registry/git/path source policy passes.
  CHECK: python3 scripts/check-release-candidate.py dependencies
  EXPECT: /release-dependencies-ok/
  EVIDENCE: Offline locked metadata reports 204 resolved packages, ten allowed SPDX identifiers, no missing license, external path, or non-registry source.

- [x] R2: Current recorded dependency advisories are resolved and the candidate contains no knowingly vulnerable pinned package.
  CHECK: python3 scripts/check-release-candidate.py advisories
  EXPECT: /release-advisories-ok open=0/
  EVIDENCE: Dependabot identified three medium tar advisories; Cargo.lock now resolves tar 0.4.46 and all three patched floors pass with open=0 in the candidate policy.

- [x] R3: Interrupted-operation recovery rejects corrupt, insecure, stale, changed, and uncertain records while preserving explicit retry/reveal/resolve behavior.
  CHECK: cargo test -p floe-app operation_recovery -- --nocapture
  EXPECT: /test result: ok/
  EVIDENCE: Seven operation_recovery tests pass over disposable roots, including corrupt/symlink/insecure storage, stale startup identity, changed source, uncertain output, success cleanup, blocked reset, and retry eligibility.

- [x] R4: Two release-source builds are byte-identical and produce verified manifest and SHA-256 output.
  CHECK: packaging/tests/test-release-candidate.sh
  EXPECT: /phase-21d-release-candidate-ok/
  EVIDENCE: Two frozen-epoch 245-file archives are byte-identical; SHA-256 verification passes. Latest measured digest after current release docs is regenerated by the gate.

- [x] R5: Documentation, packaging, isolated E2E contracts, native Wayland matrix, and full Rust quality gates pass or record exact host limitations.
  CHECK: python3 scripts/check-release-candidate.py matrix
  EXPECT: /release-environment-matrix-ok/
  EVIDENCE: Strict docs/render, package layout/migrations/source, E2E discovery, format, check, strict Clippy, 570 app tests with 14 graphical ignores, 21 app integration tests, 162 core tests, six duplicate workflows, frozen release build, and diff hygiene pass. Isolated KDE Wayland release Ping/Quit exits 0. Niri and Dogtail/pyatspi are explicitly unverified.

- [x] R6: Phase status and release claims agree, with exactly one bounded recommended next phase.
  CHECK: test "$(rg -c '\| NEXT \|' docs/ROADMAP.md)" -eq 1 && rg -n '21D.*COMPLETE' docs/ROADMAP.md
  EXPECT: /21D.*COMPLETE/
  EVIDENCE: AGENTS, roadmap, matrix, privacy/security, recovery, release matrix, changelog, README, PLAN, and ledgers mark Phase 21D complete and exactly Phase 14C NEXT.
